# Compiler Optimization = Compiler Optimization or constant folding means Python compiler gives same memory to same large values in large files.

x = 257
y = 257

print(x is y)

# Output is True in file but False in shell.